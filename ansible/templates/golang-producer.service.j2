[Unit]
Description=Golang Kafka Producer Service
After=network.target

[Service]
Type=simple
User=root
WorkingDirectory=/opt/producer
ExecStart=/opt/producer/producer
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target